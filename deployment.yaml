---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: anti-replay-demo
  namespace: anti-replay-demo
  annotations:
spec:
  replicas: 1
  selector:
    matchLabels:
      app: anti-replay-demo
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: anti-replay-demo
    spec:
      containers:
        - name: anti-replay-demo-container
          image: >-
            release.daocloud.io/skoala/envoy-extproc-anti-replay-demo-go:b96525fb68a684922449c07e8b7088f31179aee6
          ports:
            - containerPort: 50051
              protocol: TCP
          resources: {}
          terminationMessagePath: /dev/termination-log
          terminationMessagePolicy: File
          imagePullPolicy: IfNotPresent
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
      dnsPolicy: ClusterFirst
      securityContext: {}
      schedulerName: default-scheduler
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 25%
      maxSurge: 25%
  revisionHistoryLimit: 10
  progressDeadlineSeconds: 600

---
apiVersion: v1
kind: Service
metadata:
  name: anti-replay-demo
  namespace: anti-replay-demo
  annotations:
spec:
  ports:
    - protocol: TCP
      port: 50051
      targetPort: 50051
      nodePort: 31928
  selector:
    app: anti-replay-demo
  type: NodePort
  sessionAffinity: None
  externalTrafficPolicy: Cluster
  ipFamilies:
    - IPv4
  ipFamilyPolicy: SingleStack
  internalTrafficPolicy: Cluster

---


